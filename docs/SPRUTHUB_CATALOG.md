# Каталог SprutHub и правила карточек

SprutHub.Helper работает с внутренним JSON-RPC API SprutHub. Этот протокол не обещает обратную совместимость, поэтому приложение не привязывается к отображаемым именам и не считает каждую характеристику отдельным устройством.

## Иерархия данных

Каталог строится в три уровня:

1. Аксессуар — `aId`, имя, комната и сведения об устройстве.
2. Сервис — `sId`, тип, собственное имя, признаки `primary` и `linkedServices`.
3. Характеристика — `cId` и блок `control`: имя, тип, значение, чтение/запись, диапазон, шаг и `validValues`.

Адрес характеристики — только тройка `aId + sId + cId`. Одинаковые `cId` у двух сервисов одного аксессуара являются разными характеристиками. Комната, имя аксессуара и подписи могут меняться и не используются как постоянный адрес команды.

## Как несколько сервисов становятся карточками

- Внутри аксессуара Helper строит граф связей из `linkedServices`; односторонняя ссылка считается связью в обе стороны.
- Если в связанном компоненте один управляемый сервис, он становится корнем карточки, а связанные датчики — его показателями.
- Если управляемых сервисов несколько, ни один из них не удаляется и не склеивается с другим. Однозначно связанный датчик добавляется к своей кнопке; неоднозначная группа датчиков остаётся отдельной карточкой.
- Если связаны только датчики, они собираются вокруг сервиса с `primary=true`. При отсутствии такого признака используется стабильный порядок ID.
- ID карточки строится по корневому `aId/sId`. При изменении состава связей сохранённый элемент крупной панели переносится по ID любого сервиса, который реально вошёл в новую карточку.
- Неизвестная ссылка или неизвестный тип не являются поводом скрыть рабочую кнопку. Служебные сервисы вроде `AccessoryInformation` исключаются отдельно.

Такой контракт не позволяет потерять второй канал многоканального реле и одновременно не превращает температуру, влажность и батарею одного климатического устройства в ряд одинаковых плиток.

## Названия и значения

Helper использует собственное имя аксессуара, затем имя сервиса и имя характеристики. Строки могут приходить как обычный текст или как объект локализаций; выбирается язык Android с безопасным запасным вариантом.

Компактный шаблон вправе выбрать главное значение и до двух полезных показателей, но это только представление, а не фильтрация каталога. В приложении каждый многопараметрический сервис раскрывается в полный список всех распознанных характеристик без потери их исходной идентичности `aId:sId:cId`. Регрессия Qingping фиксирует восемь значений в пяти read-only сервисах: качество воздуха, PM2.5, PM10, обнаружение и уровень CO₂, батарею, температуру и влажность.

Для перечислений читаются серверные `validValues` (`value`, `key`, `name`). Это позволяет показывать фактические названия нестандартных режимов вместо числа. Для общеизвестных состояний HomeKit остаются понятные русские запасные подписи.

В разных слоях внутреннего API одно и то же значение встречается в двух формах: runtime-обёртка `value.intValue`/`value.doubleValue`/`value.boolValue` и прямое поле `value`. Helper принимает обе формы для начального каталога и событий обновления. Варианты перечисления могут называться `validValues` или `values`; они используются только как словарь подписей и не подменяют отсутствующее текущее значение первым пунктом списка.

Каталожные единицы вида `@unit_*` преобразуются одним общим словарём перед показом в приложении, виджете, плитке и Device Controls. Неизвестная единица сохраняется дословно, поэтому новый тип данных остаётся видимым, даже если для него ещё нет красивого сокращения. PM1.0 обрабатывается отдельно от PM10: после удаления служебных знаков их технические идентификаторы иначе совпадают.

## Покрытие полной JSON Schema

Помимо снимков отдельных устройств, Helper проверяется по предоставленной JSON Schema шаблонов SprutHub от 31 августа 2026 года. В ней зафиксированы 103 типа сервисов (70 стандартных и 33 `C_*`), 313 типов характеристик (240 стандартных, 67 `C_*` и 6 generic), 69 вложенных enum-контрактов и 81 допустимая единица. Большой исходный файл не копируется в репозиторий; тесты хранят только компактный [список API-идентификаторов](../app/src/test/resources/sprut-schema-types.json) без адресов, устройств и пользовательских данных.

Автоматическая регрессия состоит из шести уровней:

1. Проверяет полноту и уникальность всех 103/313 идентификаторов среза.
2. Пропускает каждый тип сервиса через глубоко вложенный ответ каталога. Пользовательский сервис обязан остаться видимым, а transport, camera-management и другая служебная инфраструктура — попасть в явный список исключений вместо случайной карточки.
3. Пропускает все типы характеристик через прямые boolean/integer/double/string и вложенные `control.value.*`. Служебный `Name` исключается осознанно; остальные значения не должны молча исчезнуть.
4. Разворачивает все 69 вложенных enum-описаний в runtime `validValues` и требует сохранить каждый серверный ключ и вариант.
5. Отдельно проверяет реальные роли управления для гаража, клапана, крана, увлажнителя, окна, двери, питания, `C_Option`, `C_TemperatureControl`, пылесоса, телевизора и вентилятора вместе с их read-only показателями.
6. Требует человекочитаемое отображение каждой locale-единицы `@unit_*`; так был найден отсутствовавший перевод `@unit_pa` в `Па`.

Schema описывает допустимые шаблоны, но не гарантирует точную форму ответа каждой версии хаба. Поэтому она дополняет, а не заменяет обезличенные runtime-снимки реальных аксессуаров. Наличие типа в списке также не означает, что Android умеет безопасно управлять каждой специализированной функцией: неизвестное значение сохраняется как read-only, а camera/transport-конфигурации не превращаются в выдуманные переключатели.

## Поддерживаемые формы управления

- boolean — включение и выключение;
- числовой диапазон — яркость, положение, скорость, температура и похожие значения;
- совместная кнопка и диапазон — например питание плюс яркость или питание плюс заданная температура;
- перечисление с `validValues` — безопасный выбор режима в крупной панели Helper с записью исходного типа значения;
- одноразовая команда — сценарий или кнопочная характеристика;
- read-only значение — показатель карточки.

Плитка и виджет не пытаются циклически переключать неизвестное перечисление: они открывают приложение, где пользователь видит все названные сервером варианты. Несколько независимых диапазонов внутри одной карточки, медиавходы и камера всё ещё требуют отдельных безопасных компонентов интерфейса. Helper показывает их текущее значение, но не подменяет выбор произвольным ползунком `0–100`.

## Шаблоны панели Helper

Визуальный шаблон выбирается по типу сервиса и роли управления, а не по пользовательскому имени:

- свет — питание, яркость и один наиболее полезный показатель;
- розетка — питание, признак нагрузки и до двух электрических показателей;
- замок, охрана и привод — предметные состояния вместо общего `включено/выключено`;
- климат — питание, заданное значение, режим и текущая температура;
- датчик — одно главное значение и до двух связанных показателей;
- сценарий — одноразовый запуск без выдуманного постоянного состояния;
- неизвестный диапазон — нейтральный регулятор с единицей и границами из SprutHub.

Простая карточка без диапазона и показателей занимает меньше высоты. Карточка с перечислением отдаёт место выбору режима и только затем заполняет оставшиеся позиции read-only показателями. Это сохраняет максимум два вторичных блока и не превращает климатическое устройство в таблицу всех характеристик; полный список остаётся доступен в раскрываемом разделе сервиса.

## Проверяемые источники

- [spruthub-client](https://github.com/shady2k/spruthub-client) описывает ответы `accessory.list`, поля `control`, диапазоны и `validValues`. Репозиторий полезен как справочник, но не считается стабильной официальной спецификацией.
- [публичный справочник сценариев SprutHub](https://github.com/sklukin/spruthub/tree/main/docs/ScenarioTemplate) подтверждает модель аксессуар → сервис → характеристика, видимость сервисов и типы/диапазоны характеристик.
- Фактический ответ актуального хаба и регрессионные тесты Helper имеют приоритет, потому что внутренний API меняется между выпусками SprutHub.
