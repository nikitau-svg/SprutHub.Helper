# Частые вопросы

## Обновляются ли виджеты, панель и плитки, если Helper закрыт

WebSocket SprutHub живёт внутри процесса Helper. Это не серверная push-подписка, способная разбудить полностью остановленное приложение: если Android уничтожил процесс или пользователь сделал принудительную остановку, соединения больше нет. После обычного смахивания окна постоянный режим телефона может продолжать держать процесс через видимое системное уведомление, но прошивка всё равно имеет право перезапустить его.

Сейчас интерфейсы обновляются по-разному:

- **Виджет главного экрана** получает изменения по WebSocket, пока процесс и соединение живы. Если процесс был обычным образом выгружен системой, Android может снова запустить его для периодического обновления виджета; заявленный интервал — 30 минут, но система и лаунчер могут его задержать. После `Force stop` таких запусков не будет до ручного открытия Helper. Кнопка обновления и нажатие для команды также проверяют кэш и при необходимости читают SprutHub.
- **Плитка быстрых настроек** перечитывает каталог, когда Android переводит её в режим прослушивания, обычно при открытии шторки. Кэш моложе 30 секунд может быть использован без полного запроса. Постоянной отдельной подписки у каждой плитки нет.
- **Стандартная системная панель Android Device Controls** запускает провайдер Helper при открытии, перечитывает устаревший каталог и получает последующие изменения, пока Android держит подписку и процесс жив.
- **Крупная панель Helper** работает как экран приложения: при открытии использует общий каталог и живые обновления активного WebSocket.

Известный пробел текущей beta: после неудачного обновления некоторые Android-интерфейсы могут сохранить последнее кэшированное состояние без достаточно заметной отметки, что оно устарело. Поэтому эта версия не обещает бесконечно актуальный статус после смерти процесса или долгой потери сети. Отдельный этап roadmap добавит единый возраст данных, состояния `онлайн / устарело / нет связи`, проверку ответа после команды и автоматическое восстановление local/cloud для каждого интерфейса.

## Что именно обновляется каждые 15 минут

Пятнадцатиминутная системная задача относится к резервной синхронизации выбранных показателей телефона с виртуальным устройством SprutHub. Это не общий интервал обновления виджетов, плиток или панели устройств. Android может отложить такую задачу из-за Doze, ограничений батареи или политики производителя.

В постоянном режиме поддерживаемые события телефона — например подключение зарядки, смена сети, экран, режим звука и будильник — отправляются сразу после системного события, пока работает постоянное уведомление. Страховочный опрос нужен для значений, у которых нет надёжного Android-события, и для восстановления после пропущенного события.

## Может ли SprutHub разбудить Helper без отдельного сервера

Обычное WebSocket-событие не разбудит принудительно остановленное приложение. Сценарий контроля heartbeat выполняется внутри SprutHub и способен отправить предупреждение через настроенный в хабе Web Push, Telegram или e-mail, даже если Helper уже не работает. Это сообщает о проблеме, но не запускает Android-приложение удалённо.

Для live-режима на Samsung следует разрешить уведомления, снять ограничение батареи и добавить Helper в список «Никогда не спящие приложения». После `Force stop` приложение нужно открыть вручную хотя бы один раз.

## Показывает ли интерфейс подтверждённое состояние после команды

Сейчас команда подтверждается ответом JSON-RPC, после чего локальный каталог оптимистично обновляется; последующее WebSocket-событие уточняет состояние, пока соединение живо. В roadmap отдельно закреплена контрольная серверная перечитка и понятное отображение неподтверждённого или устаревшего состояния, чтобы включённая иконка не могла выглядеть достоверной при потере связи.
